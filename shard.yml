name: office365
version: 1.14.0

crystal: ">= 0.36.1"

dependencies:
  connect-proxy:
    github: spider-gazelle/connect-proxy
    version: ~> 1.0

development_dependencies:
  webmock:
    github: manastech/webmock.cr
    branch: master
