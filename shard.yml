name: office365
version: 1.25.6

crystal: ">= 0.36.1"

dependencies:
  connect-proxy:
    github: spider-gazelle/connect-proxy
    version: ~> 2.0

development_dependencies:
  webmock:
    github: manastech/webmock.cr
    branch: master
