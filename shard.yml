name: office365
version: 1.9.1

dependencies:
  connect-proxy:
    github: spider-gazelle/connect-proxy
    version: ~> 1.0.0

development_dependencies:
  webmock:
    github: manastech/webmock.cr
    branch: master

crystal: 0.35.1
